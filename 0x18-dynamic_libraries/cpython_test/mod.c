#include "main.h"

/**
  * mod - a function that multiplies two integers.
  * @a: An input parameter integer
  * @b: An input parameter integer
  * Return: Always 0
*/

int mod(int a, int b)
{
	return (a % b);
}
